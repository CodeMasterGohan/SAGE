# SAGE-Docs Environment Configuration
# =====================================
# Copy this file to .env and customize for your setup.
# docker-compose will automatically load variables from .env

# ============================================================================
#  🎮 EXTERNAL GPU SERVER CONFIGURATION
# ============================================================================
# SAGE supports offloading GPU-intensive tasks to external servers.
# This is ideal for running the main SAGE stack on a CPU-only host while
# leveraging a powerful GPU server for embeddings and PDF processing.

# ----------------------------------------------------------------------------
# 📊 REMOTE EMBEDDINGS (vLLM/OpenAI-compatible server)
# ----------------------------------------------------------------------------
# Set EMBEDDING_MODE=remote in docker-compose.yml, then configure:
#
# VLLM_EMBEDDING_URL=http://your-gpu-server:8000
# VLLM_MODEL_NAME=nomic-ai/nomic-embed-text-v1.5
# VLLM_API_KEY=your-api-key-if-required

# Example: GPU server on local network at 192.168.1.100
# VLLM_EMBEDDING_URL=http://192.168.1.100:8000
# VLLM_MODEL_NAME=nomic-ai/nomic-embed-text-v1.5

# ----------------------------------------------------------------------------
# 📄 REMOTE PDF PROCESSING (olmocr server)
# ----------------------------------------------------------------------------
# olmocr uses vision-language models for high-quality PDF extraction.
# Point to a remote vLLM/OpenAI-compatible server running the OCR model.
#
# OLMOCR_SERVER=http://your-gpu-server:8000
# OLMOCR_API_KEY=your-api-key-if-required
# OLMOCR_MODEL=allenai/olmOCR-2-7B-1025-FP8

# Example: GPU server on local network at 192.168.1.100
# OLMOCR_SERVER=http://192.168.1.100:8000/v1
# OLMOCR_MODEL=allenai/olmOCR-2-7B-1025-FP8

# ============================================================================
#  📦 UPLOAD CONSTRAINTS
# ============================================================================
# MAX_FILE_SIZE=52428800        # 50MB per file
# MAX_ZIP_ENTRIES=500           # Max files in a ZIP
# MAX_ZIP_TOTAL_SIZE=209715200  # 200MB total ZIP size

# ============================================================================
#  ⚡ PERFORMANCE TUNING
# ============================================================================
# WORKER_PROCESSES=2            # Backend worker processes
# INGESTION_CONCURRENCY=100     # Concurrent indexing (remote mode)
# PDF_TIMEOUT=600               # PDF processing timeout (seconds)
